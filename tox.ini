[tox]
skipsdist = True
envlist = py26,coverage

[testenv]
passenv = ORACLE_USER ORACLE_PASSWORD
deps =
    pytest
    mock
    -r{toxinidir}/requirements.txt

commands = py.test {posargs:--tb=short}

[testenv:coverage]
deps =
    pytest
    pytest-cov
    mock
    -r{toxinidir}/requirements.txt

commands = py.test --cov=dw_lookup {posargs} 
